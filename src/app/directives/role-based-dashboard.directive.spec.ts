import { RoleBasedDashboardDirective } from './role-based-dashboard.directive';

describe('RoleBasedDashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleBasedDashboardDirective();
    expect(directive).toBeTruthy();
  });
});
